{
  "name": "ansible-role-debian",
  "version": "0.0.0",
  "private": true,
  "description": "Ansible role for a sensible default setup of a Debian server.",
  "license": "MIT",
  "author": "Visiosto oy",
  "scripts": {
    "format": "prettier --write \"**/*.{conf,json,md,types}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-packagejson": "^2.5.10"
  }
}
